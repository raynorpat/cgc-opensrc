# Generic output by Cg compiler
//vendor NVIDIA Corporation
//version 1.0.1
//profile generic
//program triangle_default
//var float4 p : $vin.POSITION : POSITION : 0 : 1
module triangle_default stage geometry
geometry input triangle vertices 3
geometry output triangle_strip max_vertices unknown
uniform struct $vin $vin;

void triangle_default(varying float4 p : POSITION)
{
  float4 c;
  p = $vin.POSITION;
  c = p;
}
