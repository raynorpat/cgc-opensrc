// Task 2 fixture: a program with no source-level modifier takes its
// geometry identity entirely from raw "-po" options (TRIANGLE and
// Vertices=3 on the command line).

void main(AttribArray<float4> p : POSITION)
{
    float4 c = p[0];
}
