// Task 4 fixture: an operation inside an assignment is not a
// complete statement.

TRIANGLE void nested_assign(AttribArray<float4> p : POSITION)
{
    float4 v = p[0];
    v = emitVertex(p[0] : POSITION);
}
