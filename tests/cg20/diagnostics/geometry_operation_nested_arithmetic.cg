// Task 4 fixture: an operation inside arithmetic is not a complete
// statement.

TRIANGLE void nested_arithmetic(AttribArray<float4> p : POSITION)
{
    float4 v = p[0];
    v * emitVertex(p[0] : POSITION);
}
