struct VertexOut {
    float4 position : POSITION;
};

VertexOut main(float4 position : ATTRIB0,
               uniform float4 values[129])
{
    VertexOut output;
    output.position = position + values[0] + values[128];
    return output;
}
