struct Params {
    float4 offset;
    float scale;
};

struct VertexOut {
    float4 position : POSITION;
};

VertexOut main(float4 position : ATTRIB0,
               uniform Params params)
{
    VertexOut output;
    output.position = position * params.scale + params.offset;
    return output;
}
