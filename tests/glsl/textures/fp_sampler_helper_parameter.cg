float4 sampleImage(sampler2D image, float2 coord)
{
    return tex2D(image, coord);
}

struct FragmentOut { float4 color : COLOR0; };
FragmentOut main(float2 coord : TEXCOORD0, uniform sampler2D image)
{
    FragmentOut result;
    result.color = sampleImage(image, coord);
    return result;
}
