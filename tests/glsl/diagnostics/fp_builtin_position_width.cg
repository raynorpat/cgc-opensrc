struct FragmentOut {
    float4 color : COLOR0;
};

FragmentOut main(float3 position : WPOS)
{
    FragmentOut output;
    output.color = float4(position.x, position.y, position.z, 1.0);
    return output;
}
