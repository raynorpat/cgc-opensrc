struct VertexOut { float4 position : POSITION; };
VertexOut main(float4 position : ATTRIB0)
{
    VertexOut output;
    float value;
    value = 0.0 / 0.0;
    output.position = float4(value, value, value, 1.0);
    return output;
}
