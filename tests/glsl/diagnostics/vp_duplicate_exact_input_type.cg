struct VertexOut {
    float4 position : POSITION;
};

VertexOut main(float2 first : TEXCOORD0,
               float3 second : TEXCOORD0)
{
    VertexOut output;
    output.position = float4(first.x, first.y, second.x, 1.0);
    return output;
}
