struct FragmentOut {
    float4 color : COLOR0;
};

FragmentOut main(float2 first : TEXCOORD0,
                 float2 second : TEXCOORD0)
{
    FragmentOut output;
    output.color = float4(first.x, first.y, second.x, second.y);
    return output;
}
