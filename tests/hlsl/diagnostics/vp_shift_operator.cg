struct VertexOut {
    float4 position : POSITION;
};

VertexOut main(float4 position : ATTRIB0)
{
    VertexOut output;
    int bits = int(position.x);
    bits = bits << 1;
    output.position = position + float(bits) * 0.0;
    return output;
}
