struct VertexOut {
    float4 position : POSITION;
};

VertexOut main(float4 position : POSITION, uniform int count)
{
    VertexOut output;
    output.position = position;
    output.position.x += float(count);
    return output;
}
