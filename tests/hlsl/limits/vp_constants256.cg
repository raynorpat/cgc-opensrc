float4 main(float4 position : POSITION,
            uniform float4 values[256]) : POSITION
{
    float4 total = position;
    int index;
    for (index = 0; index < 256; index++)
        total += values[index];
    return total;
}
