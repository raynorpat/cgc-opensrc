float4 main(float4 position : POSITION,
            uniform float4 values[257]) : POSITION
{
    float4 total = position;
    int index;
    for (index = 0; index < 257; index++)
        total += values[index];
    return total;
}
