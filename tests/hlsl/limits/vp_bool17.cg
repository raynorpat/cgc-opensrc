float4 main(float4 position : POSITION,
            uniform bool values[17]) : POSITION
{
    float total = 0.0;
    int index;
    for (index = 0; index < 17; index++) {
        if (values[index])
            total += 1.0;
    }
    return position + total;
}
