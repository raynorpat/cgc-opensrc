float4 main(float4 position : POSITION,
            uniform int4 values[16]) : POSITION
{
    int4 total = int4(0, 0, 0, 0);
    int index;
    for (index = 0; index < 16; index++)
        total += values[index];
    return position + total;
}
