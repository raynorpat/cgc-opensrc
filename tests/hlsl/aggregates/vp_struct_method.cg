struct Scale
{
    float factor;

    float twice(float value)
    {
        return value * 2.0;
    }
};

float4 main(float4 position : POSITION) : POSITION
{
    Scale scale;
    scale.factor = 2.0;
    return position + scale.factor * 0.0;
}
