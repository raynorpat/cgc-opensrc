struct VertexOut { float4 position : POSITION; };

VertexOut main(float4 position : ATTRIB0)
{
    VertexOut output;
    float annotated < float displayMin = 0.0; > = 1.0;
    output.position = position + float4(annotated, 0.0, 0.0, 0.0);
    return output;
}
