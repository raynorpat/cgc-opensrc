float4 main(float4 position : WPOS,
            float2 uv : TEXCOORD0,
            float4 color : COLOR0,
            int material : TEXCOORD1) : COLOR0
{
    return color + float4(uv.x, uv.y, (float) material,
                          position.x) * 0.0;
}
