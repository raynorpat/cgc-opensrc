float4 main(float4 position : WPOS,
            float3 uv : TEXCOORD0,
            float4 color : COLOR0,
            int material : TEXCOORD1) : COLOR0
{
    return color + float4(uv.x, uv.y, uv.z, (float) material) * 0.0;
}
